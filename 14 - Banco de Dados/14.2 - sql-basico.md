# 14.2 - SQL Básico

Exemplos de consultas SQL comuns:

- `SELECT id, name FROM users WHERE active = 1`;
- `JOIN` entre tabelas para combinar dados relacionados.

Exercício: escrever consultas para filtrar e agregar resultados (uso de `GROUP BY`).
# 14.2 - SQL Básico

Exemplos de consultas SQL comuns:

- `SELECT id, name FROM users WHERE active = 1`;
- `JOIN` entre tabelas para combinar dados relacionados.

Exercício: escrever consultas para filtrar e agregar resultados (uso de `GROUP BY`).
